<Shader>
    <OpenGL>
        <Vertex><![CDATA[
#version 330 core

layout(location = 0) in vec4 a_Position;
layout(location = 1) in vec2 a_UV;
out vec2 UV;

void main()
{
    UV = a_UV;
    gl_Position =  a_Position;
}
        ]]></Vertex>
        <Pixel><![CDATA[
#version 330 core

layout(location = 0) out vec4 color;
uniform sampler2D tex;
uniform vec2 screenResolution;
in vec2 UV;

void main()
{
    float deltaX = 1.0f/screenResolution.x;
    float deltaY = 1.0f/screenResolution.y;
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -10, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -9, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -8, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -7, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -6, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -5, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -4, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -3, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -2, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * -1, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 0, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 1, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 2, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 3, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 4, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 5, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 6, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 7, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 8, deltaY * 9));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -10));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -9));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -8));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -7));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -6));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -5));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -4));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -3));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -2));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * -1));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 0));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 1));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 2));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 3));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 4));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 5));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 6));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 7));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 8));
    color += texture(tex, UV + vec2(deltaX * 9, deltaY * 9));
    color /= 400/4;
}

        ]]></Pixel>
    </OpenGL>
</Shader>